import '../env';

// Utilities - Environment Variables - RSC usage
const { NODE_ENV, FRONTEND_URL, NEXT_PUBLIC_BACKEND_URL } = process.env;

export { NODE_ENV, FRONTEND_URL, NEXT_PUBLIC_BACKEND_URL };
