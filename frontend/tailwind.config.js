const config = {
  darkMode: 'class',
  theme: {
    extend: {
      backgroundImage: {
        star: "url('/star.svg')",
        sun: "url('./src/img/sun.svg')",
      },
      fontFamily: {
        poppins: ['Poppins', 'sans-serif'],
      },
    },
  },
};

export default config;
